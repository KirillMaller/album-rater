# CHANGELOG — R1Fрейтинг

Что изменилось для пользователя. Без технического жаргона.

Формат: новые записи сверху, дата в формате `YYYY-MM-DD`.

---

## 2026-05-27

- **Возрастная плашка 16+.** В футере рядом с «Политикой» и «Условиями» появился маленький значок «16+» в рамочке. Юридический минимум для контента с матом и тематикой 16+.
- **Средняя оценка всех зрителей.** Рядом с оценкой R1Fmabes и твоей оценкой теперь третья капсула — «Все»: средняя по всем проголосовавшим зрителям. Считается по треку и по альбому в целом (с учётом тех, кто голосовал по трекам — берётся их средняя). Обновляется живьём как только ты или другой зритель отправит голос. Показывается даже при одном голосе — сайт пока маленький, лучше что-то чем ничего. Колонки в треклисте теперь три: R1F · ТЫ · ВСЕ. У админа (Рифмабеса) колонка R1F скрыта — она бы дублировала его «Ты».
- **Одна кнопка «Сохранить мои оценки» внизу треклиста.** Раньше у каждого трека был свой «Сохранить», а блок альбома сверху имел свою кнопку. Теперь — одна большая кнопка снизу: считает все изменённые позиции (и общую альбомную, и каждый трек), при клике сохраняет всё параллельно. Несохранённые поля подсвечиваются жёлтой рамкой.
- **Превью-картинка для соцсетей и поиска.** Когда ссылку на rifmabes.ru кидают в Telegram, ВК или Google показывает в выдаче — теперь будет красивая обложка «R1Fрейтинг — Сайт с оценками» с примерами альбома/трека/баттла и кнопкой «жми — перейти на сайт». В мессенджерах превью появится сразу, в Google-поиске — через несколько дней (Google обновляет такое медленно).
- **Треклист стал аккуратнее.** Капсулы оценок выровнены по высоте, между ними вертикальные полосы вместо точек, между шапкой треклиста и первым треком — горизонтальная полоса. У интро/скитов вместо «не в счёт» теперь серая капсула «🔒 интро / скит» — сразу видно что трек заблокирован. Названия треков жирнее. Подписи R1F (cyan) / ТЫ (жёлтый) / ВСЕ (оранжевый) сверху треклиста — один раз, не повторяются у каждого трека.
- **Зрители ставят оценки.** На странице альбома и трека под обложкой появился блок «Поставь оценку». Можно поставить общую оценку альбому, можно оценить каждый трек отдельно — слайдеры под каждым треком в треклисте. Делать можно и то и другое одновременно: если двигаешь оценки треков, слайдер общей оценки сверху сразу показывает среднюю по трекам (по тем что не помечены `-`). Если ставишь свою общую оценку поверх средней — она «фиксируется» и не перетирается дальнейшим движением треков. У зрителей шкала 0..10 с шагом 0.1, у админа сохранилась пасхалка до 11. По умолчанию слайдер на 0 — сразу видно где ещё не голосовал. Кнопка «Сохранить» активна только когда есть что сохранять. Внизу треклиста есть «Сохранить все треки (N)» — параллельно отправляет все изменения за один клик. Интро/аутро/скиты, которые стример пометил `-`, голосовать нельзя — под ними просто метка «Стример исключил трек из оценки». Голос зрителя записывается в базу, второй голос от того же аккаунта обновляет первый.
- **Фильтры на главной запоминаются.** Раньше: выбрал «Альбомы», открыл альбом, нажал назад — категория сбрасывалась на «Все». Теперь: пока ты не нажал F5, выбранные категория, сортировка, период и фильтры по баттлам остаются. После F5 — сбрасывается на дефолт.
- **Чистый адрес после входа через Google.** Раньше после возврата с Google в адресной строке надолго висела длинная строка с токенами (`#access_token=...`) — это и выглядело страшно, и при повторном клике «Войти» ломало логин (токены наслаивались друг на друга, шапка зависала на «Войти»). Теперь сразу после успешного входа адрес очищается до `https://rifmabes.ru/`, а если по какой-то причине Supabase не справился с токеном — через 4 секунды страница сама уберёт мусор из адреса.
- **Согласие на обработку данных при первом входе.** После входа через Google зритель видит небольшую модалку «Чтобы голосовать»: «Нажимая «Принимаю», ты подтверждаешь, что тебе есть 16 лет и согласен с Политикой и Условиями». Две кнопки — «Позже» (модалка закроется до следующего входа) и «Принимаю» (согласие сохраняется в базе, второй раз не спросим). Это нужно, чтобы юридически зритель имел право голосовать — без согласия голос не запишется (защита включится после того, как доделаем сам интерфейс голосования). Админам тоже показывается — можно нажать «Позже» и забыть.
- **Страницы «Политика» и «Условия» — без дублей.** Раньше сверху висел заголовок «Правовая информация» и заголовок «Политика конфиденциальности», а потом сразу под ним ещё один такой же «Политика конфиденциальности» из самого текста. Лишний эйброу убран, дубль убран — теперь чистый документ.
- **Кнопка «Стереть всё» в редакторе.** В редакторе альбома, баттла и трека внизу появилась красная кнопка с корзинкой — одним кликом обнуляет все поля. Удобно после того как импортировал данные из Яндекс.Музыки (или вставил случайную ссылку) и понял, что хочешь начать с чистого листа. Перед стиранием спрашивает подтверждение, чтобы случайно не нажать. Если редактируется уже существующая запись — кнопка превращается в «Откатить правки» и возвращает форму к последней сохранённой версии.
- **Анонс концерта стал спокойнее.** Бегущую строку под шапкой сделали тусклее (приглушённые оттенки вместо ярких), а смену цвета замедлили в 3 раза — теперь она не мельтешит и не давит. Текст белый, читается чище.
- **Анонс концерта R1Fmabes в бегущей строке.** Под шапкой сайта появилась цветная полоска (фиолетовый→розовый→голубой градиент) с бегущим текстом: «🔥 7 ИЮНЯ · МОСКВА · клуб Эклипс · R1FMABES, VERCH.FATE, LEV MOVALEV · от 700 ₽ → купить билет». Клик по строке открывает страницу с покупкой билетов (vk.cc/cYaCZ7). При наведении мышкой строка замирает — удобно прочитать. Полоска автоматически исчезнет 8 июня в 00:00 МСК, руками удалять ничего не надо.
- **Название «R1Fрейтинг» с заглавной F.** Раньше во всех заголовках, шапке сайта и документации было `R1fрейтинг` со строчной f. Теперь везде заглавная F. Поправлено в шапке, во вкладке браузера и во всей внутренней документации.
- **Иконка во вкладке браузера.** Раньше во вкладке был стандартный безликий глобус. Теперь — маленькая R1-иконка в фирменном градиенте (как кубик в шапке сайта). Видна и в закладках, и в истории, и в открытых вкладках — сайт легко найти глазами.
- **Вход через Google для зрителей.** В шапке справа появилась кнопка **«Войти»**. Любой человек может войти своим Google-аккаунтом — после этого вместо кнопки в шапке будет круглая аватарка из Google и имя. Клик по бейджу открывает мини-меню с пунктом «Выйти». Это первый шаг к зрительскому голосованию — сами голоса появятся в следующем апдейте. Админы продолжают входить через `/admin/login` email+пароль, как раньше. Если Google-email админа уже привязан к админскому аккаунту — Supabase автоматически сольёт их, и можно входить через одну кнопку.
- **Политика конфиденциальности.** На каждой странице внизу появилась серая ссылка «Политика конфиденциальности» — ведёт на отдельную страницу `/privacy`. Кратко: без входа в аккаунт мы ничего о пользователе не собираем; при входе через социальный провайдер сохраняем email, имя, фото и голоса; передача рекламодателям и аналитике не ведётся; запросы на удаление аккаунта обрабатываются за 30 дней. Полный текст — на странице.
- **Условия использования.** Рядом с политикой в футере появилась вторая ссылка — «Условия использования» (`/terms`). Стандартный пользовательский договор: что сайт безвозмездный, кто и как может им пользоваться, что запрещено (накрутка, спам, противоправный контент), кому что принадлежит, что мы не отвечаем за сторонние ссылки, и куда писать претензии. Возрастная маркировка — 16+.
- **Готовится зрительское голосование.** В базе создана таблица для голосов зрителей за треки, альбомы и баттлы. На сайте пока ничего нового не видно — UI голосования появится позже.

## 2026-05-26

- **Логин в админку временно был сломан**, потом откатили. Из России сайт пробовали ускорить, проксируя запросы через свой VPS — но логин админки начал виснуть. Откатили на прямое подключение, чтобы админка снова работала. Каталог из РФ открывается на 2-3 секунды дольше чем через VPN — эту часть продолжаем чинить.

- **Треки можно исключать из средней оценки альбома.** Если в редакторе альбома поставить `-` вместо оценки трека, он останется в треклисте, но не будет участвовать в подсчёте среднего. На странице альбома такой трек помечается как «не в счёт». Это для интро, аутро, скитов и рофельных вставок, которые не хочется оценивать как полноценный трек.
- **Починили обложки баттлов с YouTube.** Прямые картинки `i.ytimg.com` у части пользователей перестали открываться, из-за этого карточки баттлов показывали пустой тёмный блок вместо превью. Теперь YouTube-обложки автоматически идут через image-proxy, а если картинка всё равно не загрузится — сайт покажет аккуратную заглушку, а не пустую карточку.

## 2026-05-18

- **Принудительный HTTPS включён.** Сертификат Let's Encrypt выпущен GitHub'ом для `rifmabes.ru`. Теперь сайт открывается только через HTTPS — если кто-то введёт `http://`, его автоматически перебросит на `https://` с замочком 🔒. Пароли при логине шифруются.
- **Иерархия для баттлов: площадка → сезон.** В метаданных баттла появились поля «Площадка» (Versus, SLOVO, Кубок МЦ, 140 BPM, …) и «Сезон» (Fresh Blood 4 и т.п.). При клике на таб «Баттлы» на главной появляется второй ряд табов с площадками; при выборе площадки — третий ряд с её сезонами. Удобно ориентироваться когда баттлов станет больше. В редакторе баттла оба поля с автокомплитом из уже занесённых записей.
- **В каталог заведено 26 черновиков FB4** (Versus: Fresh Blood 4, 2018). Пока они не опубликованы — видны только админу. Когда Кирилл проставит победителей раундов и нажмёт «Опубликовать» — карточки появятся для зрителей.
- **«11 из 10» — пасхалка.** Верхняя граница оценки теперь 11, а не 10. Если трек или альбом реально снёс крышу — можно поставить ему 11, и это сохранится. Spinal Tap-стайл, «this one goes to eleven». Ограничение на стороне БД тоже подняли до 11.
- **Дашборд переделан, чтобы каждый блок имел смысл.** Сверху три **одинаковые** featured-карточки: лучший альбом, лучший баттл, лучший трек **за всё время** (вместо «3 последних», которые дублировали каталог снизу). В правой колонке — **три блока «Топ-3 за выбранный период»** для альбомов / баттлов / треков (период берётся из главного селектора). Убраны блоки «Свежие баттлы» и «На реакции» — первый дублировал каталог, второй был бесполезен. В activity-strip внизу убрана строка «Самая высокая оценка месяца» (тоже дубль). Никаких повторов на экране, каждый блок отвечает на свой вопрос: сверху «лучшее за всё время», в центре «весь каталог», справа «лучшее за период», внизу «активность».
- **«Спорные» → «Худшие».** Третья вкладка сортировки переименована — короче и однозначно.
- **Тип-чип трека** теперь без иконки (только текст «ТРЕК»), а у альбома и баттла — иконки SVG из Lucide (диск / мечи) вместо unicode-символов. Раньше нота `♪` рендерилась криво из-за нестандартной метрики в шрифте Inter. Все три чипа теперь одинаковой ширины 72px.
- **Свой домен `rifmabes.ru`.** Теперь сайт открывается по короткому адресу без `kirillmaller.github.io/album-rater` в URL. Технически по-прежнему хостится на GitHub Pages, но снаружи это незаметно. HTTPS-сертификат GitHub выпустит автоматически через ~30 минут после привязки DNS. После этого можно будет включить принудительный HTTPS.
- **Сайт адаптируется под все размеры экранов.** Десктоп, ноутбук, планшет, телефон — везде раскладка теперь нормальная. Таблица аукционов на узких экранах скроллится горизонтально вместо переноса по строкам. На телефоне шапка превращается в колонку, кнопки в админке растягиваются на ширину экрана, бренд-логотип чуть мельче.
- **Админка приведена в порядок под стиль главной.** Заголовок «Админка» уменьшен (был слишком большой). Табы «Альбомы / Баттлы / Треки» — теперь овальные pill-кнопки с фиолетово-магента градиентом для активного, как на главной. Счётчики записей — в круглых капсулах.
- **Дата выхода + дата оценки.** В редакторе альбома, баттла и трека есть поле «Дата оценки». При создании новой записи автоматически проставляется сегодня (Москва). Если знаешь точную дату когда оценил — можно поставить руками задним числом. На карточках и странице записи теперь видно обе вещи: когда релиз вышел (`вышел: 2024`) и когда был оценён (`оценён: 12 мая 2026`).
- **Поля оценок: точка ИЛИ запятая, без скролла мышью.** Раньше когда листал страницу с открытым полем оценки — колёсико случайно меняло значение. Теперь колёсико снимает фокус, цифра не сбивается. Можно вводить и `5.5`, и `5,5` — оба варианта сохраняются как 5.5. Стрелочки-спинеры справа от поля убраны.
- **Все даты на сайте — в московском времени.** Если показываем дату или время сохранения чего-либо — теперь однозначно МСК. Зрители в любом часовом поясе видят одно и то же.
- **Подробные сообщения об ошибках.** Если сохранение записи падает — теперь в красной плашке показывается реальная причина (например, «дубликат slug», «нет прав на запись»), а не общее «Не удалось сохранить запись». Полная инфа об ошибке также пишется в консоль браузера для отладки.
- **Маленькая кнопка сброса черновика → совсем убрана.** Сначала большая кнопка «Сбросить локальный черновик» уменьшилась до иконки-корзинки, потом вся плашка «Локальный черновик автосохранён в этом браузере» убрана с глаз. Автосохранение работает в фоне без UI-уведомлений.
- **Аукционы — отдельный раздел сайта.** В шапке появились две новые ссылки: **«Аукционы»** и **«Правила»**. Аукционы — это очередь записей, на которые зрители скидывают донаты: чем больше собрано на запись — тем раньше R1Fmabes её разбирает. Шесть категорий: альбомы, сериалы, фильмы, аниме, игры, баттлы. Переключаются вкладками, чтобы не листать длинный список. По умолчанию открываются «Альбомы». В очередь сразу залиты 41 запись со старых табличек: 26 альбомов, 10 аниме, 5 сериалов. Категории «фильмы», «игры» и «баттлы» пока пустые. Несколько записей (Киберпанк, Необъятный океан) помечены «Категория под вопросом — уточнить», поправь через админку. **Админ видит дополнительно**: кнопку «+ Добавить» над таблицей и иконки «Изменить» / «Удалить» у каждой строки прямо на публичной странице — не нужно прыгать в админку. Также есть отдельная страница `/admin/auctions` с тем же функционалом, плюс редактор правил.
- **Подсветка «зависших» аукционов.** Если запись с маленькой суммой (≤ 500 ₽) долго не обновляется (никто не докидывает донаты) — она автоматически подсвечивается. **Жёлтым** через 3 месяца с подписью «давно не обновлялся (3 мес.)». **Красным** через 6 месяцев с подписью «давно не обновлялся (6 мес.), можно убрать». Это подсказка что зрители скорее всего «забыли» про эту запись и её проще удалить чем держать в очереди. Таймер обнуляется когда правишь сумму через UI — БД сама ставит свежий `updated_at`. Сейчас все записи только что засеяны, поэтому подсветки пока не видно — появится со временем.
- **Главная страница — переделана под макет редакторской газеты.** Сверху три **лучшие карточки по типам**: лучший альбом, лучший баттл и лучший трек в выбранном периоде. Обложка занимает всю карточку, текст лежит поверх неё, а бейдж типа («альбом», «баттл», «трек») стоит в правом нижнем углу обложки и не съедает место у названия. Ниже — **поиск + сортировка** (новые / лучшие / худшие), **период** (за всё время / за месяц / за 7 дней / в этом году / за прошлый год) и переключатель **оценённые / вышедшие**. Справа от сетки — боковая колонка с лучшими альбомами, баттлами и треками по той же логике периода. Карточки стали чуть светлее, а фон сайта — мягче.
- **Шапка сайта — переделана под макет.** Логотип теперь квадрат «R1» с градиентом cyan→purple + жирное «R1Fрейтинг» рядом. Навигация: **Каталог · Аукционы · Правила · Админка · Выйти** (последние два — только для админов). Заголовок «Каталог» на главной уменьшен (был слишком большой). Большой счётчик «опубликовано» рядом с заголовком убран.
- **Админские кнопки скрыты от обычных пользователей, но доступны быстро админу.** Раньше любой зритель видел в шапке ссылку «Админка» и на главной кнопку «Начать оценку» — это могло сбить с толку. Теперь это видно **только когда залогинен админ**. Админ при этом может нажать «Править» прямо на карточке каталога, большой карточке или странице записи, без поиска нужного элемента в списке.
- **Зафиксировали правила сохранности данных.** Перед передачей каталога R1Fmabes написали полный runbook [docs/DATA_SAFETY.md](DATA_SAFETY.md): где живут данные, что их может разрушить, как делать снапшоты, как восстанавливаться. Главное — обновления фронта НЕ трогают данные. Папка `db/snapshots/` для локальных JSON-снапшотов теперь существует (gitignored).
- **Селекторы вместо ручного ввода жанра.** В редакторе альбома и трека вместо текстового поля «Жанр» теперь выпадающий список (Рэп, Поп, R&B, Рок, Электроника, Инди, Метал, Экспериментальный) + опция «Свой» для редких случаев. У баттла поле жанра скрыто — он автоматически «Баттл-рэп». Заодно у баттла появилась полноценная классификация: **Формат** (1 на 1 / 2 на 2 / 3 на 3 / Triple Threat / Дезматч / Свой), **Стиль** (A Capella / BPM / BPM + A Capella / Фристайл / Тематический / Свой), **Стадия** (текст — 1/4, Финал, Fresh Blood, Титульный). YouTube-парсер заполняет формат и стадию автоматически. Меньше опечаток, проще каталогизировать и потом фильтровать.
- **Импорт баттлов с YouTube.** В редакторе при типе «баттл» появился блок «Импорт с YouTube» — вставляешь ссылку на ролик (Кубок МЦ, Versus, и т.п.) → автоматически заполняются название, обложка, стороны A и B (если в названии есть `vs`), плюс ссылка на оригинал. Дезматч-формат (БОЛЬШЕ ЧЕМ БАТТЛ, N участников через `x`) распознаётся, но требует ручного дополнения — модель данных пока знает только две стороны.
- **Новая версия сайта (React + Supabase + Яндекс-импорт) задеплоена на прод.** Раньше на `kirillmaller.github.io/album-rater/` была старая одностраничная HTML-версия — просто калькулятор без сохранения. Теперь там полноценный каталог: можно заводить альбомы / треки / баттлы, ставить оценки, публиковать. Данные хранятся в облачной базе.
- **Импорт из Яндекс.Музыки заработал на проде.** Вставляешь ссылку → нажимаешь «Импортировать» → форма заполняется: название, артист, год, жанр, треклист, **обложка**. Работает и на локалке, и на боевом сайте. Проверено в браузере: альбомы и треки сохраняются корректно. Баттлы тоже сохраняются — пока заполняются руками (импорт баттлов с YouTube запланирован отдельной задачей).
- Заведена документация проекта: корневой `CLAUDE.md`, `docs/ARCHITECTURE.md`, `docs/BACKLOG.md`, `docs/CHANGELOG.md`, спринт в `team/sprints/`. Зафиксировано продуктовое видение: сайт для стримера R1Fmabes, в перспективе — лёгкий логин для зрителей и две независимые оценки (стримера и зрителей).
