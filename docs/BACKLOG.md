# BACKLOG — R1fрейтинг

Что нужно сделать в проекте, по приоритету. Обновляется по ходу.

## Легенда

- 🔴 **P0** — блокирует выпуск / прод, делать в первую очередь.
- 🟠 **P1** — заметно влияет на пользователя, делать скоро.
- 🟡 **P2** — улучшение, без него можно жить.
- 🟢 **P3** — идея на потом, ещё не решено что делаем.

---

## 🔴 P0

### Передача каталога Рифмабесу

Перед тем как давать ссылку с правами админа — пройти чек-лист в [docs/DATA_SAFETY.md](DATA_SAFETY.md) → секция «Передача каталога Рифмабесу». Кратко:

1. ~~Удалить тестовые записи~~ — сделано Кириллом 2026-05-18.
2. Сделать локальный JSON-снапшот данных (точка возврата на «момент перед запуском»).
3. Завести Рифмабесу собственный аккаунт Supabase Auth, добавить его в `admin_users` (не отдавать ему пароль Кирилла).
4. Проверить что сессия Кирилла и Рифмабеса отдельные, ничего не путается.

---

## 🟠 P1

### Голосование зрителей (лёгкий логин + один голос с акка)

**Цель:** зрители могут ставить свою оценку трекам и выбирать победителя в раундах баттлов. С одного аккаунта — один голос на сущность.

**Требования:**

- Лёгкий способ зайти. Кандидаты: Google OAuth через Supabase Auth, VK OAuth, magic-link на email без пароля. Главное — **не пароль**, чтобы зритель не отваливался на регистрации.
- Один аккаунт = один голос на трек / на раунд / на итог баттла. Голос можно поменять, но не суммируется.
- Защита от накрутки на уровне аккаунта.
- В БД: новая таблица `viewer_votes` (item_id, track_id или round_id, viewer_id, score / winner_side), с уникальным индексом.

**Голосование по типам контента:**

- Альбом / трек — оценка по 10-балльной.
- Standard баттл — победитель раунда (A / B / ничья) + итог.
- Дезматч-баттл (см. отдельный пункт ниже) — выбор одного победителя из N.

**Открытые вопросы:** какой провайдер логина выбрать; нужны ли ограничения по возрасту/региону; что считать «голосом» для баттла — только итог, или каждый раунд тоже.

### Две независимые оценки на карточке: стример vs зрители

После голосования (см. выше) — на странице записи показывать **две оценки рядом**:

- Слева — оценка Рифмабеса (`final_score`).
- Справа — средняя оценка зрителей (из `viewer_votes`).

Для баттлов аналогично: «победитель по Рифмабесу» vs «победитель по большинству зрителей».

### Онлайн-баттл — отдельный тип записи

Кирилл прямо обозначил (2026-05-18) что онлайн-баттл — это **особый формат**, у него **другие правила оценки** и должна быть **другая карточка**, не как у обычного баттла с раундами. Поэтому делаем как отдельный `ItemType`, не как подформат.

**Что такое:** соревнование без живого выступления — каждая сторона выкладывает свой материал (треки-диссы) в соцсетях. Структура: «два мини-альбома один против другого».

**Что нужно:**

- В `ItemType` добавить значение `'online_battle'`.
- Миграция БД: расширить check-constraint `rated_items.type` (это будет миграция `003_add_movie_anime_online_types.sql` если идём с фильмом/аниме одним пакетом, иначе `003_add_online_battle.sql`).
- Метаданные: стороны A и B, плюс **два списка треков** (как у альбома, но раздельно для каждой стороны). Раундов нет — оценка по каждому треку + общая.
- В редакторе при выборе типа «Онлайн-баттл» — форма с двумя секциями «Сторона A: треки» / «Сторона B: треки» и общим победителем.
- На странице записи — рендер «двух треклистов рядом» с оценками. Победитель — сводно.
- В каталоге — отдельная вкладка / фильтр для онлайн-баттлов.

**Открытые вопросы:**
- Какой источник для импорта? YouTube (один общий ролик), отдельные ссылки на YT-видео каждой стороны, или ссылки на треки в Яндекс.Музыке?
- Кто выигрывает — выбор Рифмабесом, или по сумме оценок треков?

### Фильтры и сортировка в каталоге

Чтобы «было видно какой круче из всех» (формулировка Кирилла) — каталог должен поддерживать фильтрацию:

- По **типу записи** (альбом / баттл / трек / онлайн-баттл / фильм / аниме).
- По **формату баттла** (1v1 / 2v2 / дезматч / …).
- По **стилю баттла** (a capella / BPM / фристайл / тематический / …).
- По **жанру** (для альбомов и треков).
- По **стадии турнира** (Финал / 1/4 / Титульный / …).
- По **наличию реакции Рифмабеса** (есть видео-реакция или нет).
- Сортировка по оценке Рифмабеса, по дате релиза, по дате добавления. После задачи 04 — сортировка по оценке зрителей.

UI: панель фильтров сверху каталога, чипы выбранных фильтров с возможностью убрать.

Глобально это значит «топ а капелла», «топ дезматчей у Рифмабеса», «лучшие альбомы 2024», и т.п.

### Дезматч-баттлы (N участников все против всех)

Сейчас модель `BattleMetadata` в [src/main.tsx](../src/main.tsx) поддерживает только классический баттл из двух сторон A/B. Но в реальности есть **третий формат** — **дезматч**: N участников, все против всех, побеждает один. Канал «БОЛЬШЕ ЧЕМ БАТТЛ» (DEATHMATCH SEASON) делает именно такой формат.

**Пример:** `БОЛЬШЕ ЧЕМ БАТТЛ | T!MMI x Hidji 17 x AVENUE SS х HPPD х АГЕНТ ДАН` — 5 участников, все против всех.

**Что нужно:**

- В тип `BattleMetadata` добавить поле `format: 'standard' | 'deathmatch'` (default `'standard'`).
- Для `standard` — текущая модель (sideA, sideB, rounds, finalWinner).
- Для `deathmatch` — новые поля: `participants: BattleParticipant[]` и `winner: string` (id одного из участников). Раунды для дезматча — опциональны, обычно дезматч это один заход.
- В редакторе [src/main.tsx](../src/main.tsx) — переключатель формата при создании баттла. Для дезматча — список участников (можно добавлять/удалять), выбор победителя.
- В голосовании зрителей (см. отдельная P1) — для дезматча голос = «кто из N победил», не «A/B/ничья».

**Миграция БД:** ничего не нужно — `metadata` это `jsonb`, новое поле живёт там же.

<!-- Год публикации YouTube-роликов осознанно не делаем (2026-05-18) — Кирилл решил
     что год баттла на видео всё равно часто врёт (ролик мог быть залит позже),
     лучше ставить руками. -->


### Новые типы контента: фильм и аниме

Рифмабес смотрит фильмы и аниме по донатам зрителей. Сейчас типы записи — только альбом / баттл / трек.

Что нужно:

- Добавить в `ItemType` значения `'movie'` и `'anime'`.
- Миграция БД: расширить check-constraint на `type`.
- Поля специфичные хранить в `metadata jsonb`.
- В редакторе [src/main.tsx](../src/main.tsx) — новые опции в селекторе типа и формы.
- Импорт по ссылке: Kinopoisk / IMDb / Shikimori / MyAnimeList — отдельная задача ниже.

### Импорт по ссылке для других платформ

Яндекс уже работает (через VPS-прокси). Расширить идею на другие источники:

- **Apple Music / iTunes** — Search API, без авторизации, обложки качественные. **Самый простой кандидат**, не требует VPS-прокси (геоблока нет).
- **Spotify** — Web API, нужен client_id/secret, OAuth-данс. Метаданные отличные.
- **Bandcamp** — нет публичного API, парсить HTML.
- **Kinopoisk / IMDb** для фильмов, **Shikimori / MyAnimeList** для аниме.

Логика: фронт определяет источник по `new URL(input).hostname`, фронт зовёт соответствующий импортёр. Яндекс — через VPS, остальные могут быть прямо в браузере (если CORS позволяет) или через тот же VPS как унифицированный шлюз.

---

## 🟡 P2

### Разбить `src/main.tsx`

Сейчас это монолит на ~1300 строк. Когда продукт устоится — разбить минимум на:

- `types.ts` — все типы.
- `pages/Catalog.tsx`, `pages/Item.tsx`, `pages/Editor.tsx`, `pages/AdminList.tsx`, `pages/Login.tsx`.
- `lib/storage.ts` — обёртка над `localStorage` / Supabase.
- `lib/yandexImport.ts` — клиентская часть импорта.

Пока делать **не нужно** — продукт ещё меняется.

### Тесты и линтер

Сейчас нет ни одного. Минимум: ESLint + prettier, vitest для парсера в `server/yandex-proxy/index.mjs` (там реальная логика). Полноценный e2e — позже.

### Зачистить лишние Supabase access-токены

В аккаунте Supabase висит 7+ токенов от прошлых попыток `supabase login` (имя `cli_HOME-PC\GOD@HOME-PC_*`, expiry «Never»). Это риск — если утечёт, доступ бесконечный. Зайти на https://supabase.com/dashboard/account/tokens и **отозвать все кроме того, которым реально пользуемся** (`R1Frating CLI`, expiry 30 дней).

### Зачистить тестовые записи в проде

На 2026-05-18 в `rated_items` лежит тестовая запись `type='battle', title='кеп'`, явно мусор. Удалить через админку или прямым SQL.

### Мониторинг прокси

Сейчас если `yandex-proxy.service` или Caddy упадут — узнаем только когда кто-то нажмёт «Импортировать» и получит ошибку. Поставить uptime-мониторинг (например, UptimeRobot бесплатный) на `https://195.208.3.209.sslip.io/health`.

### Переключить миграции на Supabase CLI

Сейчас миграции 001 и 002 накатаны вручную через SQL Editor — Dashboard про них «не знает» (показывает «No migrations»). Если когда-нибудь будет третья миграция (для фильмов/аниме например) — стоит перенести `db/migrations/*.sql` в `supabase/migrations/` и катить через `supabase db push`. Чтобы был учёт.

---

## 🟢 P3

- Тёмная / светлая тема (сейчас только одна).
- Публичная RSS / OPML лента новых оценок.
- Шеринг карточек в соцсети с превью (og:image).
- Комментарии / реакции от анонимов (Disqus или своё на Supabase).
- Передать админский аккаунт самому Рифмабесу, чтобы он заводил карточки сам, а не через Кирилла.
- Свой домен вместо sslip.io (выглядит профессиональнее, не привязано к IP — если IP сменится, переключение проще).

---

## ✅ Сделано в SPRINT-001 (для исторического контекста)

- **Импорт Яндекс.Музыки на проде через VPS-прокси.** Vite dev-плагин выкинут, всё ходит через `https://195.208.3.209.sslip.io/yandex-music/import`. Обложка тянется.
- **Миграции 001 и 002** уже накачены на проде (Кирилл прогнал через SQL Editor до начала спринта). Таблицы `rated_items`, `track_scores`, `media_links`, `admin_users` живые, тип `track` и колонка `metadata jsonb` работают.
- **Закоммитили миграцию проекта на React/Vite и пушнули.** 6 коммитов в `main`, GitHub Actions deploy ✓ success, прод обновлён.
- **Импорт баттлов с YouTube** через oEmbed напрямую из браузера. Парсер покрывает три формата (standard / team / deathmatch) и 8 проанализированных реальных видео. Дезматчи определяются, но полная поддержка модели — отдельной задачей.

---

## 🔗 Якоря и ссылки

- Прокси для Яндекса: [server/yandex-proxy/](../server/yandex-proxy/) + [server/yandex-proxy/README.md](../server/yandex-proxy/README.md).
- Описание импорта в архитектуре: [docs/ARCHITECTURE.md → Импорт из Яндекс.Музыки](ARCHITECTURE.md#импорт-из-яндексмузыки).
- Базовая схема БД: [db/migrations/001_init.sql](../db/migrations/001_init.sql).
- VPS: `bot-napominalka` на reg.cloud, IP `195.208.3.209`, прокси-URL `https://195.208.3.209.sslip.io`.
- Прод сайта: `https://kirillmaller.github.io/album-rater/`.
- Репо: `https://github.com/KirillMaller/album-rater`.
- Supabase Dashboard: `https://supabase.com/dashboard/project/nfekasqbzwjelrwyxqmv`.
